import styles from './HeroBanner.module.css';
import Button from '../commons/Button/Button';

const { 'banner-container': bannerContainer, 'banner-content': bannerContent } =
  styles;
const HeroBanner = () => {
  return (
   
  );
};
export default HeroBanner;
